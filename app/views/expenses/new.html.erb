<div class="title-return">
  <h1>New expense</h1>
  <%= link_to group_expenses_path(@group), :class => "return-icon" do %>
    <i class="fa-solid fa-chevron-left action-icon"></i>
  <% end %>
</div>

<%= render "form", expense: @expense %>
